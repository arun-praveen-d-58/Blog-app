<%= render 'layouts/messages' %>

  <h2 class="text-center mt-4">Posts with Tag <%= "'#{@tag.name}'" %></h2>
  <div class="wrap mt-4" >
<%= link_to :"Back", posts_path , class: "btn btn-outline-dark"%>
<div style="height: 25px"></div>

<% @tag.posts.each do |post| %>
  <%= render post%>
      <div style="height: 30px;"></div>
<% end %>
  </div>
</div>

